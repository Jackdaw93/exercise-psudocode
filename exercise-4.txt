STORE "nilai"

IF "nilai" > 80
    DISPLAY "A"
ELSE IF "nilai" < 80 AND > 70 
    DISPLAY "B"
ELSE IF "nilai"  < 70 AND > 60
    DISPLAY "C"
ELSE IF "nilai" < 60 AND > 50
    DISPLAY "D"
ESLE 
    DISPLAY "E"
ENDIF